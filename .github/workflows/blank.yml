name: action

on:
  [ push, pull_request ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Git clone 
        uses: actions/checkout@v2
      - name: Build
        run: |
          echo "Testeous test"
          ls -la 
          echo "Build success"
